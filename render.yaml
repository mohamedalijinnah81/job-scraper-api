services:
  - type: web
    name: job-scraper-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn job_scraper_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
